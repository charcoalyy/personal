import React from 'react';
import Header from './Header';

function App() {
  return (
    <div className="App">
      <Header />
    </div>
  );
}

export default App;

// remember to use as HTML(X)Element to get properties if document selector
// remember to import from javascript files